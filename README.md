# file-manager
This is file manager script adopted for zsh mac
Need to install: 
    brew install chafa
    brew install ranger
    brew install screen
    brew install --cask screens
    brew install peco